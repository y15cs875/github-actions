name: My Frist Workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My First github action"
        
        - name: List files
          run: ls
        
        - name: Read File
          run: cat README.md